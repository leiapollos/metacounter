#include "player.h"

// This tests the duplicate detection.
const auto DUMMY_PLAYER_HEALTH_REG = REGISTER_COUNTER(PlayerHealth);

void Player::TakeDamage(int amount) {
    // ... logic for taking damage
}